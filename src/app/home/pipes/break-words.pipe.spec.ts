import { BreakWordsPipe } from './break-words.pipe';

describe('BreakWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new BreakWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
